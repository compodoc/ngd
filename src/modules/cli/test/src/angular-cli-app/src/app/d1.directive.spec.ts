import { D1Directive } from './d1.directive';

describe('D1Directive', () => {
  it('should create an instance', () => {
    const directive = new D1Directive();
    expect(directive).toBeTruthy();
  });
});
