import { D2Directive } from './d2.directive';

describe('D2Directive', () => {
  it('should create an instance', () => {
    const directive = new D2Directive();
    expect(directive).toBeTruthy();
  });
});
