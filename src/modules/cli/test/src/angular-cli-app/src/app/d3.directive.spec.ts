import { D3Directive } from './d3.directive';

describe('D3Directive', () => {
  it('should create an instance', () => {
    const directive = new D3Directive();
    expect(directive).toBeTruthy();
  });
});
